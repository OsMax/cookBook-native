import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: 64,
    bottom: 0,
    alignItems: "center",
    borderColor: "#000",
  },
  text: {
    color: "#fff",
  },
});
