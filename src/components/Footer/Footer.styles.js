import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    // flex: 0,
    // justifyContent: "center",
    width: "100%",
    height: 64,
    // position: "absolute",
    bottom: 0,
    alignItems: "center",
    borderColor: "#000",
  },
  text: {
    color: "#fff",
  },
});
